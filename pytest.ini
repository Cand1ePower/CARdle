[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
timeout = 30
markers =
    unit: 单元测试，不依赖任何外部服务
    integration: 集成测试，需要后端服务运行
    e2e: 端到端测试，需要前端+后端全量启动
    slow: 运行时间较长的测试（> 10s）
addopts =
    -v
    --tb=short
